/**
 * Centralized configuration for patch-skills
 * Patch filename format: skills+<skill-name>.patch
 */
export const config = {
  skillsDir: '.agents/skills',
  patchesDir: 'patches',
  skillFilename: 'SKILL.md',
  patchPrefix: 'skills+',
  patchExtension: '.patch',
};
